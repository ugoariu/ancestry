{% extends 'TefNetwebAncestryBundle:Default:index.html.twig' %}

    {% block  content_content -%}
        {% block headline %}
            {{ 'Ancestry Death edit'|trans }}
        {% endblock headline %}
        <ul class="record_actions">
            <li>
                <a href="{{ path('death') }}">
                    {{ 'Back to the list'|trans }}
                </a>
            </li>
            <li>
                <a href="{{ path('death_show', { 'id': entity.id }) }}">
                    {{ 'Show'|trans }}
                </a>
            </li>
        </ul>

        {{ form(edit_form) }}

        <ul class="record_actions">
            <li>
                <a href="{{ path('death') }}">
                    {{ 'Back to the list'|trans }}
                </a>
            </li>
            <li>
                <a href="{{ path('death_show', { 'id': entity.id }) }}">
                    {{ 'Show'|trans }}
                </a>
            </li>
            <li>{{ form(delete_form) }}</li>
        </ul>
    {% endblock %}
