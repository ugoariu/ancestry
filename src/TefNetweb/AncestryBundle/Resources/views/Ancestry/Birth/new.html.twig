{% extends 'TefNetwebAncestryBundle:Default:index.html.twig' %}

{% block  content_content -%}
    {% block headline %}
        {{ 'Ancestry Birth creation'|trans }}
    {% endblock headline %}
    <ul class="record_actions">
        <li>
            <a href="{{ path('birth') }}">
                {{ 'Back to the list'|trans }}
            </a>
        </li>
    </ul>
    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('birth') }}">
                {{ 'Back to the list'|trans }}
            </a>
        </li>
    </ul>
{% endblock %}
